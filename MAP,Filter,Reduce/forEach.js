// forEach -  Used for iterating over an array & perform side effects (like logging or modifying external variables) and
//you don't need a return value.

let number = [1,2,3,4]
number.forEach((num) =>{
  console.log(num*2))
